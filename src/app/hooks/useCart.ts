// Re-export the useCart hook from the context
export { useCart } from '../context/00CartContext';

// You can add additional cart-related hooks here in the future
// For example:
// export const useCartItem = (id: number) => { ... }
// export const useCartTotal = () => { ... }
// export const useCartCount = () => { ... } 